{
  "question": "What is preventing this pull request from merging?",
  "trigger": ["A pull request has unresolved review or CI state"],
  "decision": ["Assign the next unblock action", "Declare the pull request ready to merge"],
  "evidence": ["Pull-request metadata", "Review state", "CI check results"],
  "success_measure": "Every active blocker has an evidence-backed owner and next action",
  "outcome": "Produce an actionable pull-request blocker summary",
  "environment": {
    "documentation_sources": ["Pull-request provider API documentation"],
    "data_sources": ["Pull-request metadata, reviews, and CI status"],
    "required_capabilities": ["Read pull-request state"],
    "readiness_checks": ["The pull request exists and is readable"]
  },
  "risk": {"tier": "low", "permissions": ["Read pull-request state"], "mutation_boundary": "read-only", "approval_required": []},
  "software_mutation": {"applies": false},
  "data_interfaces": {"applies": true, "interface_types": ["api"], "authoritative_sources": ["Pull-request provider API documentation"], "entities": ["Pull request", "Review", "CI check"], "identifiers": ["Pull-request number", "Review ID", "CI check ID"], "relationships": ["Reviews and CI checks belong to one pull request"], "field_semantics": ["Provider status values retain their documented merge semantics"], "invariants": ["Every reported blocker references its source review or CI check"], "freshness_and_pagination": ["All pages are read and status is refreshed before the summary"], "nullability": ["Optional reviewer and completion timestamps remain distinct from missing records"], "readiness_checks": ["One safe pull-request response matches the documented provider schema"]},
  "semantic_contract": {"applies": false},
  "routing_tests": {
    "should_trigger": ["What blocks PR 42?", "Summarize unresolved pull-request checks", "Is this pull request ready to merge?"],
    "should_not_trigger": ["Merge PR 42", "Write release notes", "Review this issue backlog"]
  }
}
