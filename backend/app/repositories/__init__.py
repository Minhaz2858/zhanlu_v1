"""Repository layer — data access objects wrapping SQLAlchemy queries.

Each repository module provides typed create/read/update/delete operations
for its model, plus paginated list and search helpers.
"""
